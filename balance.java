public class balance {
        //commit
public static void main( String[]args ) {
        int balance = 100;
        int amount = 0;
        String Text = "";
        while ( balance > 0 ) (
                text ="Your balance: \u0024" + balance + "." ;
                text += "\nAmount to withdraw:";

                System.out.printIn(text);
                // User Input
                Scanner input = new Scanner(System.in);
                amount = inptut .nextInt();
                balance -= amount;
}

}